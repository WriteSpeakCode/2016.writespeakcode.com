%section.content
  %h1
    Write/Speak/Code 2016 Talks

  %h2
    Keynotes (#{@keynotes.count})
  .speakers
    =render partial: 'talk', collection: @keynotes

  %h2
    Conference Sessions (#{@both_talks.count})
  .speakers
    = render partial: 'talk', collection: @both_talks

  %h2
    1st Year Track (#{@first_year_talks.count})
  .speakers
    = render partial: 'talk', collection: @first_year_talks

  %h2
    Alumni Track (#{@alumna_talks.count})
  .speakers
    = render partial: 'talk', collection: @alumna_talks